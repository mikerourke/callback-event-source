# Callback Server with EventSource

Example demonstrating how to use `EventSource` with a callback server.

## Getting Started

- Run `npm install`
- Run `npm watch:all`
- Navigate to `http://localhost:8080` in the browser